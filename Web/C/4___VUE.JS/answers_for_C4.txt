to run server => npm run serve

C4.2
1) Как соотносятся между собой nodejs и npm?
===npm — это node package manager, то есть пакетный менеджер для node, и он входит в станадартный пакет установки node
2) После установки у нас в исходниках появился конфигурационный файл, в котором описаны все необходимые зависимости. В каком формате описан этот файл?
===это обычный json, похожий на то dict в Python

C4.3
1) Добавьте в App.vue ещё один блок template (просто скопируйте существующий) и замените "Welcome to Your Vue.js App" на "Привет". Дождитесь пересборки проекта и посмотрите, что получится. Будет ли работать такой код?
===будет только русский текст

C4.4
1) О каком типе ошибки сообщит линтер при сборке компонента, если нарочно убрать два слова из кода "export default"
===Syntax Error

C4.5
1) В качестве идентификатора задачи мы использовали постоянно увеличивающийся int. А какую другую функцию стандартной библиотеки можно использовать для генерации уникальных идентификаторов?
===uuid.uuid4 верно

C4.6
1) В документации есть более подробное описание функций условной отрисовки (https://vuejs.org/v2/guide/conditional.html)
В Python есть конструкция elif. А как аналогичный флаг называется во Vue?
===v-else-if

C4.7
1) Для отображения таблицы мы использовали конструкцию v-for. В Python для того, чтобы, например, перебрать числа от 1
до 10, мы используем конструкцию for n in range(1, 11). В документации по Vue на странице https://vuejs.org/v2/guide/list.html
описано, как обходить списки в шаблоне.
А как будет выглядеть условие для v-for, где переменная цикла принимает значения последовательно от 1 до 10?
===v-for="n in 10"